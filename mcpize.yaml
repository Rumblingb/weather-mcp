# MCPize manifest - https://mcpize.com
# Generated by mcpize analyze
version: 1
name: weather-mcp
description: Get current weather and 5-day forecasts for any city — directly from AI agents via MCP protocol.
runtime: python
entry: server.py
build:
  install: pip install -r requirements.txt
startCommand:
  type: stdio
  command: python
  args:
    - server.py
