runtime: "container"
startCommand:
  type: "stdio"
  command: "node"
  args: ["index.js"]
description: "Live weather, forecasts, and severe-weather alerts for any city on Earth. The weather context every travel, logistics, and scheduling agent is missing."
